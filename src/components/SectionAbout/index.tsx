// styles
import * as Styled from './styles';

// types
export type SectionAboutProps = {
	bios: string;
};

const SectionAbout = ({ bios, }: SectionAboutProps) => {
	return (
		<Styled.Wrapper>
		</Styled.Wrapper>
	);
};

export default SectionAbout;
